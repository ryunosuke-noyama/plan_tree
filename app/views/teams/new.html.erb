<%= render "shared/header1" %>

<div class= 'team-main'>
  <h1 class= 'main-header'>+Team</h1>
  <%= form_with model: [@company,@team], class: 'form', local: true do |f| %>
    <div class="form-input">
      <%= f.text_field :team_name, class: 'form-company', placeholder: 'type a team name' , autocomplete: 'off'%>
    </div>
    <div class='member_select'>

    </div>
    <%= f.submit '追加', class: 'form-submit' %>
    <%= link_to 'Back', company_path(@company), class: 'btn'%>
  <% end %>
</div>