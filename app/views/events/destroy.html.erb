<div class="contents row">
  <div class="success">
    <h3>削除が完了しました。</h3>
    <%= form_with model: [@company, @plan, @event], class: 'form', local: true do |f| %>
    <%= link_to 'Back', company_plan_path(@company.id, @plan.id), class: 'btn'%>
  <% end %>
</div>